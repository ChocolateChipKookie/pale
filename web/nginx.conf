server {
    listen 80;
    server_name pale.crvukamenu.work;

    root /var/www/pale;
    index index.html;

    # Serve the pre-built index.html.br directly (requires ngx_brotli).
    brotli_static on;

    location / {
        try_files $uri $uri/ =404;
    }
}
